name: CDCbooker
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - nodejs  # required for Playwright installation (browsers)
  - pip:
      - playwright>=1.35.0,<2.0.0
      - playwright-stealth>=1.0.0
      - python-telegram-bot>=20.0
      - APScheduler>=3.9.0
      - python-dotenv>=1.0.0
      - pydantic>=2.0.0
      - orjson>=3.8.0
      - prometheus-client>=0.16.0
      - loguru>=0.7.0
